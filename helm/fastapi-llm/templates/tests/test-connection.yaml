apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fastapi-llm.fullname" . }}-test-connection"
  labels:
    {{- include "fastapi-llm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fastapi-llm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
